{
  "name": "@rezamonangg/pi-worktree",
  "version": "0.1.1",
  "publishConfig": {
    "access": "public"
  },
  "type": "module",
  "keywords": ["pi-package", "pi", "worktree"],
  "description": "Pi package that routes agent file and shell work into an isolated git worktree.",
  "pi": {
    "extensions": ["./extensions/pi-worktree/index.ts"],
    "skills": ["./skills"]
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --test --import tsx extensions/pi-worktree/*.test.ts"
  },
  "devDependencies": {
    "@types/node": "latest",
    "tsx": "latest",
    "typescript": "latest"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-ai": "*",
    "@earendil-works/pi-tui": "*",
    "typebox": "*"
  }
}
